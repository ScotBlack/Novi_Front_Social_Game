

function Player(props) {
    return (
        <>
            <article className={"player red"}><h2 className={"player_name"}>{props.name}</h2></article>
            <article className={"player red"}><h2 className={"player_name"}>{props.name}</h2></article>
        </>
    );
}

export default Player;